from django.contrib import admin
from .models import NetworkingContact, WeeklyActivityTarget

# Register your models here.
admin.site.register(NetworkingContact)
admin.site.register(WeeklyActivityTarget)
